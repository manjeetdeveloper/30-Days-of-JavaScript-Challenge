/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var filter = function(arr, fn) {
    //return arr.filter(function(element, index) {
        //return fn(element, index);
    //});

   // here try next logic in this problem let's start'

   return arr.filter(fn);
    
};